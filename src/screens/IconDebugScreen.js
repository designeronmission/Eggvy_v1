import React from 'react';
import { SafeAreaView } from 'react-native';
import IconDebug from '../components/IconDebug';

const IconDebugScreen = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <IconDebug />
    </SafeAreaView>
  );
};

export default IconDebugScreen;